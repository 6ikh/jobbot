# requirements.txt
# Install with: pip install -r requirements.txt
 
# Apify client — runs LinkedIn scraper on Apify's cloud
apify-client==2.5.0
 
# For sending Discord webhooks
requests==2.31.0
 
# For loading .env file when running locally
python-dotenv==1.0.1
 
